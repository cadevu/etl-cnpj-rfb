# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/

# uv
# (uv.lock IS committed on purpose — reproducible installs)

# Data — bronze/silver are large and reproducible from source, never commit them
data/bronze/*.zip
data/_work/
data/silver/

# Local secrets / env
.env

# Airflow local runtime (Fase 3)
airflow/logs/
airflow/plugins/__pycache__/

# Editors / OS
.vscode/
.idea/
.DS_Store
*:Zone.Identifier

# Claude Code local (machine-specific, not shared)
.claude/settings.local.json
